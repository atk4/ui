<div class="{PaginatorType}ui pagination menu{/}">
    .left.floated.left.aligned.ten.wide.column
      <{_element}div{/} {$attributes}>
      |
      | {rows}

      | {Item}
      a(class="item {$active}" href="{$link}" data-page="{$page}") {$page}
      |
      | {/}

      | {Spacer}
      .disabled.item ...
      |
      | {/}

      | {/rows}
      </{_element}div{/}>
    .right.floated.right.aligned.six.wide.column
      | {$afterPaginator}
</div>
= "\n"
